Original prompt: i want to make a game. ive been building it in chatgpt right now and we are still in the early stages. tell me what you need from em to continue. the file i will give you is the general blueprint/idea/scope/style/ etc of the game.

- 2026-04-06: Started a systems pass for versioning, input-mode separation, gameplay settings, and combat feel cleanup.
- Goal for this chunk: centralize visible version info, add gameplay/options hooks that stay data-driven, and separate desktop/touch presentation so future content drops stay plug-and-play.
- Implemented: centralized build/version info (`v0.0.11`), input mode preference + auto-detect plumbing, gameplay difficulty presets, desktop-only mission toolbar, desktop free-aim cleanup, and lighter decorative density for hub/mission scenes.
- Verification: `npm run build` passes. Local Playwright verification reached `hub` from `main-menu` and debug state reported `touchMode: false` on desktop path. Canvas screenshots were black in Playwright even in headed mode, so the structured `render_game_to_text` hook was added as a reliable fallback for future automated checks.
- TODO next: use the new gameplay/input settings in a broader UI polish pass, and expand automated coverage deeper into the mission flow once the browser client can drive the prototype controls more directly.
- 2026-04-06 follow-up: bumped to `v0.0.12` for a reliability pass covering mission fail flow, mission-complete return flow, auto-fire on auto-aim lock, and companion hub anchoring.
- Verification: `npm run build` passes. Custom local Playwright run verified mission-complete returns to `hub`, auto-aim acquires a `rusher` and spawns bullets automatically, `Continue?` from `game-over` restarts the mission from stage 1, and `Return To Ship` requeues the contract in the hub. Standard web-game client run also reached `hub` cleanly after the update.
- 2026-04-06 follow-up 2: bumped to `v0.0.13` and moved mission completion into its own scene so the end-of-mission return button no longer depends on mission-scene input layering.
- 2026-04-06 follow-up 3: finished the `v0.0.13` mission-complete fix by removing the leftover in-scene result overlay, routing completion through a dedicated `mission-result` scene, and adding a `?renderer=canvas` switch for reliable local automation on Windows headless browsers.
- Verification: `npm run build` passes. Local Playwright verification in canvas mode confirmed `mission -> mission-result -> Return To Ship -> hub` works end to end, and the standard web-game client also ran successfully against the local preview build.
- TODO next: ship/hub layout still has overlapping text in a few places (notably the reward banner vs. header messaging in the command deck), so the next UI cleanup pass should space those regions more intentionally.
- 2026-04-06 follow-up 4: bumped to `v0.0.14` and converted mission-finish/game-over flows from paused mission overlays into full scene handoffs so phone touch input no longer competes with the mission scene underneath.
- Verification: `npm run build` passes. Mobile-touch Playwright verification in canvas mode confirmed a tap on `Return To Ship` from the mission-complete screen returns to the hub with `touchMode: true`.
- 2026-04-06 follow-up 5: bumped to `v0.0.15`, simplified mission touch controls to one floating move/facing stick plus a dedicated attack button, and added a lightweight story/arcade run-config foundation so future arcade mode has a stable rules layer without exposing half-built menus yet.
- Verification: `npm run build` passes. Mobile-touch verification in canvas mode confirmed one-stick movement updates both move and facing vectors, and manual attack still fires correctly with auto aim disabled. Standard web-game client check also passed.
- 2026-04-06 follow-up 6: bumped to `v0.0.16`, loosened the touch stick anchor so it can spawn across nearly the full screen, and split the controls menu into `Keyboard / Mouse` and `Touch` subtabs with real touch sensitivity tuning.
- Verification: `npm run build` passes. Mobile canvas verification confirmed the move/facing stick anchors at `x=980, y=230` on the right side, and the same thumb motion produces a stronger move vector at `140%` touch sensitivity than at `100%`. Standard web-game client check also passed.
- 2026-04-06 follow-up 7: bumped to `v0.0.17`, brought the hub touch stick in line with mission behavior, added a shared interaction layer (`Activate` on touch, `E` prompt on desktop), fixed the fail -> return -> redeploy hub state bug, and moved the conflicting combat ability off `E` so interact is the stable use key going forward.
- Verification: `npm run build` passes. Local Playwright verification confirmed desktop station interaction opens with `E`, fail -> return to ship requeues the mission, redeploy works on the next attempt, touch-mode interaction shows the contextual activate button in the hub, and the ship stick logic now accepts right-side anchoring with touch sensitivity applied. Standard web-game client run also passed.
- Notes: graphics settings are live but intentionally subtle right now (quality density, brightness, screen shake, hit flash). Audio sliders persist correctly, but we do not have enough real music/SFX hooked up yet for those changes to feel dramatic. Mouse sensitivity is still scaffolded in UI only; touch sensitivity is the active control tuning.
- 2026-04-06 follow-up 8: bumped to `v0.0.18`, changed the ship deploy door from automatic walk-through to an explicit confirm/decline interaction, added cooldown shading to touch buttons and desktop ability cards, and made rest-room combat lockouts read clearly as `Safe Room` instead of feeling like broken input.
- Verification: `npm run build` passes. Local Playwright verification confirmed standing in the deploy door no longer starts the mission automatically, `E`/activate opens a deploy confirmation panel with `Start Mission` and `Decline`, and mission HUD cooldown states now show seconds plus a dark depleting overlay in both rest rooms and normal combat cooldowns. Standard web-game client run also passed.
- TODO next: a lightweight logbook is starting to make sense soon because accepted/active/completed mission state now matters in more than one place. Inventory can still wait until loot and equipment depth are a bit further along.
- 2026-04-06 follow-up 9: bumped to `v0.0.19`, fixed mission touch buttons so their hit areas stay pinned with the camera instead of ghost-triggering from the left side in scrolled rooms, restored `Arc Lance` to the `E` key in missions, and renamed the visible game title to `Circle of Light`.
- Verification: `npm run build` passes. Focused mobile Playwright verification in a scrolled hallway confirmed left-side ghost taps no longer trigger abilities, the visible right-side `Pulse` button fires correctly, the button becomes usable again after cooldown expires, and the mission toolbar now reads `E | Ready` for Arc Lance.
- 2026-04-06 follow-up 10: bumped to `v0.0.20`, split `auto aim` and `auto fire` into separate saved settings, added shared target cycling (`Tab` on desktop, `Target` button on touch), introduced shield resources for player/companion/high-tier enemies, and made companions targetable with a downed/revive loop instead of being untouchable support drones.
- Verification: `npm run build` passes. Local Playwright browser verification confirmed: player auto-fire stays idle until a manual target lock exists when `autoAim=false`/`autoFire=true`; `Tab` and the touch target button both acquire nearby targets; player shields absorb damage before health; late-stage shooters spawn with shields and lose shield before HP; companions can be downed and auto-revive with partial HP/shield. Screenshot artifacts were saved to `output/web-game/v0.0.20-verify/desktop.png` and `output/web-game/v0.0.20-verify/touch.png`.
- TODO next: a lightweight logbook/mission journal still makes sense soon, and the mission HUD/ship layout could use another spacing pass before we add more story-facing UI on top of it.
- 2026-04-06 follow-up 11: bumped to `v0.0.21` and replaced the passive companion auto-revive with a manual revive channel. Desktop now uses `hold F` near a downed companion, and touch repurposes the top-right target button into a hold-to-revive button while the companion is down and in range. Taking damage during the channel cancels the revive progress.
- Verification: `npm run build` passes. Local Playwright verification confirmed companions stay down indefinitely without input, `hold F` revives them on desktop, the touch revive button also revives them when held, and both flows return the companion with partial HP/shield. Screenshot artifacts were saved to `output/web-game/v0.0.21-revive/desktop-revive.png` and `output/web-game/v0.0.21-revive/touch-revive.png`.
- 2026-04-06 follow-up 12: bumped to `v0.0.22`, slowed shield recovery into a more Halo-like reset-on-hit window, moved touch revive duty onto the main attack button, added visible HP/shield numbers to player and companion bars, shortened the companion bars, reduced this starter companion's HP/shield pool, changed the lock target into player-colored corner brackets, and tightened movement feel so the player/companion/enemies read less floaty.
- Verification: `npm run build` passes. Local Playwright verification confirmed the longer shield delay does not start regenerating too early and resets when hit again, touch shows revive on the attack button while keeping the target button for targeting, player/companion bar numbers render, companion bars remain shorter than player bars, targeting color matches the player color, and the new grounded stride/follow behavior updates during movement. Screenshot artifacts were saved to `output/web-game/v0.0.22-feel/desktop-feel.png` and `output/web-game/v0.0.22-feel/touch-feel.png`.
- 2026-04-06 follow-up 13: bumped to `v0.0.23`, added a mission-only `Return To Ship` option in pause, rebuilt mission companions into a two-member squad with a tank ally (`Rook`) beside `Sera`, and tightened revive flow so touch only replaces `Attack` with `Revive` when a downed companion is actually in range.
- Verification: `npm run build` passes. Focused local browser verification confirmed mission pause abandon returns to `hub` with the contract requeued, the mobile attack button stays `Attack` while a downed companion is out of range, flips to `Revive` when in range, taking damage cancels the revive channel, and the mission squad now spawns with two differently colored companions where the tank has the larger HP pool.
- 2026-04-06 follow-up 14: bumped to `v0.0.24`, moved companion definitions into shared content data, turned `Rook` into a proper shield-vanguard with a visible guard plate and front-line positioning, and brought the full current squad into the ship crew-quarters and loadout console so hub/missions use the same roster source.
- Verification: `npm run build` passes. Focused local browser verification confirmed the hub now shows both `Sera` and `Rook`, the loadout console lists their current roles/gear/stats from shared companion data, and mission state reports `Rook` as the `shield` companion with the visible guard plate enabled.
- 2026-04-06 follow-up 15: bumped to `v0.0.25`, replaced the ship-door launch confirm with a real deploy-prep screen, added saved squad slot assignments (`front/back/left/right`) with role validation, exposed the deploy screen even when no mission is accepted, and made mission companions spawn/follow from the chosen formation slots instead of the old hardcoded pattern.
- Verification: `npm run build` passes. Local browser verification against the preview build confirmed: `New Game -> hub`, deploy prep opens from the ship door before mission acceptance, mission acceptance persists back in the hub, squad assignments can be cleared/rebuilt on the deploy board, and launching the mission carries the chosen `front-right` / `back-right` slots into mission runtime. Artifacts saved under `output/web-game/v0.0.25-deploy/`.
- TODO next: the deploy board is functional but still wants a layout polish pass so underlying hub text reads less through the overlay and the future seven-character roster can scale cleanly. The next natural feature step is the full squad-selection/details screen pass (player centered, richer stats/gear readout, and more companion roster support).
- 2026-04-07 follow-up 16: bumped to `v0.0.26` and gave the deploy screen its first real hierarchy pass. The modal is darker, the roster portraits are now the visual focus, the formation sockets are smaller/lower, and hover/selection now drives colored light-beam treatment that can later sit behind real character art instead of placeholder silhouettes.
- Verification: `npm run build` passes. Local browser capture confirmed the new default deploy layout plus the hover state for `Sera`, with artifacts saved under `output/web-game/deploy-polish-pass-3/`.
- TODO next: once real assets start landing, swap the placeholder figures for character art and tune the roster row spacing for 5-7 visible companions. The next structural UI step is still the fuller squad-details screen with gear/stat readout around the formation board.
- 2026-04-07 follow-up 17: bumped to `v0.0.27` and built the first real command-layer milestone. The hub now supports three contracts (`easy`, `medium`, `hard`) through a mission terminal + logbook split, the loadout bench is now a real inventory/crafting foundation, the deploy door launches the currently active accepted mission, and the mission generator now produces mixed horizontal/vertical layouts with difficulty-shaped pacing.
- Implemented: expanded companion roster to six color-coded allies (`green`, `red`, `blue`, `yellow`, `purple`, `orange`) with distinct attack styles; data-driven mission contract generation in `src/game/content/missions.ts`; inventory/equipment/item scaffolding in `src/game/content/items.ts`; save/session support for accepted missions, selected mission, cargo, crafting materials, and equipment in `src/game/core/session.ts`; new overlays for the mission terminal, mission log, and loadout bench; hub layout cleanup with a cockpit bridge placeholder and better crew-quarter placement; deploy overlay routing tied to selected mission + formation assignments; mission loot burst and combat-light polish in the mission scene.
- Verification: `npm run build` passes. The runtime verifier in `tools/verify_command_layer_runtime.mjs` drove `main menu -> hub -> mission terminal/logbook/loadout -> deploy -> mission` and produced fresh screenshots plus `output/web-game/command-layer-runtime/summary.json`. That run confirmed all 3 contracts can be queued, `nightglass-abyss` can be selected as the active route, the loadout bench opens with equipment/cargo/material data, the deploy screen stays functional with the expanded roster, and the mission scene starts with randomized `up/right` stage flows plus the chosen squad slots.
- Notes: the command-layer UI is now structurally solid, but the loadout bench still has some mild spacing debt around the gear-heading area and the bench materials copy. That is now polish debt, not a missing-system blocker.
- TODO next: decide whether the next milestone should be `command layer polish + better art framing`, `second mission content/boss depth`, or `healer/tank/companion AI refinement`. Inventory interaction is still read-only for now; equipping/crafting behavior has not been turned on yet.
- 2026-04-07 follow-up 18: bumped to `v0.0.28` and turned the mission log into a real datapad flow instead of a ship terminal. The save beacon terminal is gone, the loadout bench moved into the command line row, the terminal circles were removed, and both hub + mission scenes now expose the Data Pad through a button plus `L` on keyboard.
- Implemented: hub terminal cleanup and even spacing in `src/game/scenes/HubScene.ts`; shared datapad access in `src/game/ui/LogbookOverlay.ts`, `src/game/ui/MissionBoardOverlay.ts`, and `src/game/scenes/MissionScene.ts`; deeper companion combat identity in `src/game/scenes/MissionScene.ts` (self/ally healing, support beams, tank shield pulses, ranged volley, melee gap-cleave, caster chain arcs, demolition splash); and a full loadout-bench layout rebuild in `src/game/ui/InventoryOverlay.ts` so the screen reads more like a real inventory board instead of a text slab.
- Verification: `npm run build` passes repeatedly. `node tools/verify_command_layer_runtime.mjs` passes after the refactor and now confirms the datapad overlay opens from the hub while the loadout bench remains functional. Local Playwright client runs were also executed via `node tools/web_game_playwright_client.mjs ...` against the preview build after requesting browser-launch approval.
- Notes: the new loadout screen is visually much stronger, but it is still read-only. Gear drag/drop, crafting actions, and companion-specific equipment panes are still future work. Companion roles now look different in code/HUD and should feel more distinct in combat, but the next round of playtesting should focus specifically on healer value and whether the tank feels protective enough.
- TODO next: if the user likes this direction, the next clean milestone is either `deeper combat identity pass` (companion tactics, enemy reactions, healer/tank tuning) or `command layer expansion` (actual inventory interactions, crafting actions, and better deploy/loadout stat detail).
- 2026-04-07 follow-up 19: bumped to `v0.0.29` and added a dedicated `UI Vision` mockup scene so the visual style can be reviewed before the real UI kit refactor starts. This pass does not replace live menus yet; it is a direction board built inside the actual game shell.
- Implemented: a reusable first-pass UI theme helper layer in `src/game/ui/theme.ts`; a new `src/game/scenes/UiVisionScene.ts` with three preview pages (`Main Menu`, `Data Pad`, `Inventory`); animated rainbow-ring hero treatment for the main menu mockup; a command-layer datapad mockup with icon rail + mission list/detail split; and a central-well inventory mockup that leaves the character presentation intentionally blank until final art is ready. Main menu now links to the mockup scene through a `UI Vision` button and `V` key.
- Verification: `npm run build` passes. Browser verification passed through both `node tools/web_game_playwright_client.mjs --url http://127.0.0.1:4173/?renderer=canvas --click 286,612 --iterations 1 --pause-ms 250 --screenshot-dir output/web-game/ui-vision-client` and `node tools/verify_ui_vision.mjs`, which captured `main-menu.png`, `data-pad.png`, and `inventory.png` in `output/web-game/ui-vision/`.
- Notes: this is the seed of the future UI kit, not the full migration. The next step, if approved, is to refactor the actual live screens to use this shared chrome style instead of scene-by-scene custom drawing.
- 2026-04-07 follow-up 20: bumped to `v0.0.30` and redid the UI Vision sandbox as a proper reference pass instead of a cleaned-up prototype pass.
- Implemented: removed the floaty backdrop circles from `src/game/ui/theme.ts`; rebuilt `src/game/scenes/UiVisionScene.ts` so the three pages follow the user's references much more directly; main menu now uses a calmer premium layout with a cleaner ring frame and less stray FX; Data Pad now follows the icon-rail/list/detail composition more tightly; and the inventory mockup now mirrors the corrected slot logic from the reference with the character well centered and the cargo board anchored below it.
- Verification: `npm run build` passes. Browser verification passed again through `node tools/web_game_playwright_client.mjs --url http://127.0.0.1:4173/?renderer=canvas --click 286,612 --iterations 1 --pause-ms 250 --screenshot-dir output/web-game/ui-vision-client` and `node tools/verify_ui_vision.mjs`, producing fresh screenshots in `output/web-game/ui-vision/`.
- Notes: this still is not the live UI migration. It is the stronger visual target pass that should be approved before I refactor the real menus to use it.
- 2026-04-07 follow-up 21: bumped to `v0.0.31` and rebuilt the live inventory overlay around layout and interaction instead of styling flourishes.
- Implemented: replaced the old mixed `loadout bench` screen in `src/game/ui/InventoryOverlay.ts` with a cleaner split layout: equipment on the left, clear character display on the right, three accessory slots centered under the character, full-width cargo grid at the bottom, and a footer with status text + currency. Added shared equip validation helpers in `src/game/content/items.ts` and the first real cargo-to-slot equip action in `src/game/core/session.ts`.
- Verification: `npm run build` passes. Browser verification passed through `node tools/web_game_playwright_client.mjs --url http://127.0.0.1:4173/?renderer=canvas --click 286,372 --iterations 1 --pause-ms 250 --screenshot-dir output/web-game/inventory-layout-client` and the new focused `node tools/verify_inventory_overlay.mjs`. The focused verifier captured the new inventory layout and confirmed a cargo item can be equipped into a valid slot, leaving the cargo slot empty afterward.
- Notes: this is a layout-first foundation, not the final skin. It now gives us a stronger structural model to copy into other menus before we do the broader UI migration.
- 2026-04-07 follow-up 22: bumped to `v0.0.32` and fixed the mission queue flow so queued contracts and active contracts are separate states instead of being silently merged.
- Implemented: updated `src/game/core/session.ts` so accepting one or all contracts no longer auto-activates a mission, starting/abandoning a mission clears the active selection instead of silently promoting the next queued route, and loading a save preserves `no active contract selected` instead of forcing the first queued route active. `src/game/ui/MissionBoardOverlay.ts` and `src/game/ui/LogbookOverlay.ts` were also updated so they communicate the new queue-vs-active flow more clearly.
- Verification: `npm run build` passes. A focused local Playwright browser run against the preview build confirmed the exact desired sequence: fresh new game starts with `accepted=[]` and `selected=null`; all 3 mission-terminal cards can be queued; after each accept the active mission remains `null`; and the Data Pad can then explicitly set `nightglass-abyss` or `outpost-breach` active on demand. Artifacts were saved under `output/web-game/mission-queue-fix/`.
- Notes: if any player still reports “only the middle mission works,” that would now point to a device-specific tap target issue rather than bad queue logic, because the local browser verification exercised the top, middle, and bottom contract actions successfully.
- 2026-04-07 follow-up 23: bumped to `v0.0.33` and added a procedural retro combat SFX layer instead of hardcoded audio files.
- Implemented: new reusable synth-based sound manager in `src/game/audio/retroSfx.ts` with user-gesture audio unlock, SFX/master volume support from saved settings, and debug counters. `src/game/createGame.ts` now installs the unlock hook and exposes sound debug state. `src/game/scenes/MissionScene.ts` now triggers distinct cues for player fire, pulse, arc lance, dash, healer/shield/ranged/melee/caster/demolition companion actions, enemy shots, boss burst, shield hit/break/recharge, loot burst, and companion revive.
- Verification: `npm run build` passes. Focused local Playwright browser checks confirmed the audio context reaches `running` and that gameplay hooks increment the expected cue counters: player fire, pulse, arc lance, dash, healer cast, guard pulse, ranged volley, plus dedicated shield break and shield recharge events. Artifacts/logs were written under `output/web-game/retro-sfx/`.
- Notes: this is intentionally a layout-free, asset-free audio foundation. Later we can replace or layer these procedural cues with hand-authored samples without changing combat logic, and we can eventually route the same shield cues into ship systems once ship shields are live.
- 2026-04-07 follow-up 24: bumped to `v0.0.34` for a cleanup pass focused on performance, Data Pad filtering, and roster readability.
- Implemented: reduced UI churn in `src/game/ui/buttons.ts` and `src/game/scenes/MissionScene.ts` by avoiding repeated no-op label updates and throttling heavy HUD text refreshes; added transient FX budgeting in `MissionScene` so combat lights/beams/loot bursts cannot stack infinitely on slower devices; cleaned up Web Audio node lifetimes in `src/game/audio/retroSfx.ts`; rebuilt `src/game/ui/LogbookOverlay.ts` so the Data Pad only shows active/accepted missions plus a collapsed completed section; and promoted `Orin` into a second healer in `src/game/content/companions.ts` while adding role symbols that now display in hub, deploy, and mission HUD screens.
- Verification: `npm run build` passes. `node tools/verify_cleanup_pass.mjs` confirmed a fresh save starts with an empty Data Pad (`accepted=[]`, `selected=null`), queued routes only appear after acceptance, and the roster now reports two healers. `node tools/verify_command_layer_runtime.mjs` also passed as a broader regression check. Screenshots/artifacts were saved under `output/web-game/cleanup-pass/` and `output/web-game/command-layer-runtime/`.
- Notes: the new Data Pad behavior is correct, but the overlay still has some visual overlap from the underlying hub UI showing through its translucent shell. If we return to command-layer polish later, that left rail wants one more spacing/opacity pass.
- 2026-04-07 follow-up 25: bumped to `v0.0.35` for a QoL + companion role pass.
- Implemented: completed story missions now disappear from the mission terminal while still living in the Data Pad archive; deploy prep gained per-companion `I` popovers driven by richer companion metadata (bio, attack summary, ability summary, gear); pause now exposes a real fullscreen checkbox row and mission resume requests pointer lock when possible; companion definitions were refactored into visual identities plus combat kits so current colors are only temporary presentation and not permanent role logic; the temp roster now sits at `2 DPS / 2 Tanks / 2 Healers`; healers no longer attack and instead split into burst-heal (`Lyra`) and focus-buff (`Orin`) support; `Ember` was converted into the second tank with a cleansing shockwave role; and mission combat now uses soft actor separation so player/companions/enemies stop collapsing into one stack as often.
- Verification: `npm run build` passes. New focused verification in `tools/verify_qol_role_pass.mjs` confirmed: completed missions are removed from terminal listings after clear, deploy info popovers render with the new metadata, roster role counts are `2/2/2`, `Lyra` heals, `Orin` grants focus buffs, `Ember` applies the new guard buff, soft separation meaningfully increases actor spacing, and the browser fullscreen toggle activates in Playwright. Artifacts were saved under `output/web-game/qol-role-pass/`.
- Notes: fullscreen activation is verified in-browser, but pointer lock still reported `false` in headless Playwright even though the request path is wired in on mission resume and gameplay clicks. There is also still layout polish debt in the mission terminal/detail pane and the deploy info popup positioning, but the underlying systems now work.
- 2026-04-07 follow-up 26: bumped to `v0.0.36` for the web/focus-mode and mission HUD cleanup pass.
- Implemented: save-slot deletion with confirmation in `src/game/ui/SaveSlotsOverlay.ts` plus slot-removal support in `src/game/core/session.ts`; pause fullscreen was reframed as browser `Focus Mode` in `src/game/scenes/PauseScene.ts`, and mission-side pointer-lock forcing was removed so desktop mouse aiming still works while focused; mission HUD readability got a spacing pass in `src/game/scenes/MissionScene.ts` with a separate player buff row, larger gaps between companion stacks, names lifted above the bars, companion effect rows under each unit, a centered boss bar, nearest-target lock behavior for `Tab`/touch targeting, and color-coded mission tracker dots for hallway/rest/boss stages; and long-session churn was reduced by lowering HUD refresh frequency, lowering transient FX budgets, trimming stage star density, and adding an active-voice budget in `src/game/audio/retroSfx.ts`.
- Verification: `npm run build` passes. `node tools/verify_focus_hud_pass.mjs` passes and confirms save delete prompt + removal works, nearest target lock selects the closer enemy, browser focus mode activates with `pointerLocked: false`, companion HUD rows are vertically separated, and mission tracker dots now carry distinct hallway/rest/boss colors. Artifacts were saved under `output/web-game/focus-hud-pass/`.
- Notes: on the web build, browser `Esc` can still exit focus mode because the browser owns that key in fullscreen. To soften that, `MissionScene` now opens pause when focus mode drops during active play, but true `Esc = pause without leaving fullscreen` is still a better fit for the later desktop app build than for browser delivery.
- 2026-04-07 follow-up 27: bumped to `v0.0.37` for the combat behavior and aggro pass.
- Implemented: added a real threat/aggro layer in `src/game/scenes/MissionScene.ts` so damage, support, and tank tools now influence who enemies focus; tank kits now include short taunt windows plus an `Aegis` invulnerability state instead of passively restoring shields; healer/support AI now waits for actual pressure windows before casting instead of firing instantly on cooldown; healer abilities were retuned to heal health only (not shields) and split more clearly into burst-heal vs. focus-support behavior; companion positioning was tightened so healers stay back, tanks hold front space more deliberately, and avoidant movement reacts more intelligently to nearby enemies/projectiles; and the mission HUD got another spacing pass with names lifted further above the bars and more room between companion stacks.
- Verification: `npm run build` passes. `node tools/verify_combat_behavior_pass.mjs` passes and confirms the wider HUD spacing, verifies `Lyra`/`Orin` abilities no longer modify shields while still restoring health/buffs, confirms `Rook` gains `Taunt` + `Aegis`, and shows enemy focus shifting onto the taunting tank while incoming damage is ignored during the active Aegis window. Artifacts were saved under `output/web-game/combat-behavior-pass/`.
- Notes: this pass improves combat intelligence and pressure management, but it is not the boss/enemy move overhaul yet. The next major combat milestone is still the real encounter framework: enemy single-move kits, multi-phase bosses, and more distinct boss difficulty pools.
- 2026-04-07 follow-up 28: bumped to `v0.0.38` for the first real boss/enemy move framework pass.
- Implemented: mission boss stages in `src/game/content/missions.ts` now carry boss identity by difficulty instead of assuming one brute forever (`shard-bruiser`, `relay-seer`, `nightglass-behemoth`), and `src/game/scenes/MissionScene.ts` now reads that data into a proper encounter layer. Standard enemies now have clearer signature moves (`Rend Pounce` for rushers and `Suppressive Volley` for shooters), companion HUD name rows were lifted further above their bars with larger spacing between companion blocks, and bosses now have three sequential phases with color-shifting health bars, per-phase move pools, and difficulty-specific archetype behavior. The current boss move set includes `Crusher Slam`, `Nova Ring`, `Shadow Call`, `Fan Volley`, and `Beam Sweep`, and the boss bar/title now update immediately on spawn and phase shifts.
- Implemented (audio): `src/game/audio/retroSfx.ts` now includes unique retro cues for the new enemy/boss moves (`enemy-pounce`, `enemy-volley`, `boss-slam`, `boss-fan`, `boss-summon`, `boss-beam`, `boss-phase`) so new combat mechanics continue to follow the rule that every meaningful move gets its own sound hook.
- Verification: `npm run build` passes. `node tools/verify_boss_enemy_pass.mjs` passes and confirms the lifted HUD spacing, verifies rusher/shooter move labels and behavior (`rusher-windup` -> `rusher-lunge`, volley spawning 4 shots on hard), and confirms the hard-tier boss (`Nightglass Behemoth`) spawns with the correct title, advances cleanly through phases 1/2/3, changes boss-bar color by phase, and rotates through different phase moves (`Beam Sweep`, `Crusher Slam`, `Fan Volley`). Artifacts were saved under `output/web-game/boss-enemy-pass/`.
- Notes: this is the framework pass, not the final encounter pass. Bosses now phase and use real moves, but they still want more authored pacing, telegraph polish, and additional boss pools per difficulty. Long-session performance also still wants a deeper profiling pass rather than more guesswork tweaks.
- 2026-04-08 follow-up 29: bumped to `v0.0.39` for the mission refresh + combat tactics pass and updated the visible game naming to `LoE / Pocket Legends: / The Circle of Light`.
- Implemented: shared branding constants in `src/game/core/buildInfo.ts` plus updated main-menu/UI-vision/browser titles; story contracts now use an `exhausted` cycle state in `src/game/core/session.ts` and `src/game/ui/MissionBoardOverlay.ts`, which means finished routes disappear from the terminal until the new `Refresh Routes` action repopulates the board; `src/game/content/missions.ts` now increases later-room enemy counts, slightly reduces baseline enemy durability, and introduces a third controller enemy type (`hexer`) into medium/hard pressure groups and boss adds; and `src/game/scenes/MissionScene.ts` now uses enemy-pressure-based formation direction for companions instead of the player aim vector, adds simple cover props with actor collision + bullet blocking, adds the `Shadow Hexer`/`Dread Hex` move with a slow debuff, spaces companion HUD rows further, and tunes support/guard uptime so buffs do not sit at near-permanent uptime.
- Implemented (audio): softened the main player-fire timbre and added a dedicated `enemy-hex` retro cue in `src/game/audio/retroSfx.ts`, keeping the project rule that every meaningful new move gets its own SFX hook.
- Verification: `npm run build` passes. `node tools/verify_refresh_tactics_pass.mjs` passes and confirms exhausted mission boards show the refresh state then repopulate to 3 routes, companion formation pressure points toward enemies even when aim points the other way, the new `hexer` creates a debuff bullet, cover spots exist and block bullets, and the browser title reflects `Pocket Legends: The Circle of Light`. Artifacts were saved under `output/web-game/refresh-tactics-pass/`.
- 2026-04-08 follow-up 30: bumped to `v0.0.40` for a healer behavior hotfix between fights.
- Implemented: `src/game/scenes/MissionScene.ts` now lets healers keep topping off allies outside combat instead of treating the absence of enemy pressure as a reason to stop casting. The direct healer now considers even light missing HP between waves, and the support healer will still cast for small heal-up situations instead of waiting only for combat pressure windows.
- Verification: `npm run build` passes.
- 2026-04-08 follow-up 31: bumped to `v0.0.41` for the loot foundation + title hierarchy pass.
- Implemented: replaced placeholder player gear/cargo with real item instances in `src/game/content/items.ts`; added mission/boss loot generation and enemy salvage drops in `src/game/content/loot.ts`; converted mission contracts to reward previews plus generated boss caches in `src/game/content/missions.ts`; migrated save/session handling in `src/game/core/session.ts` to empty-start equipment, inventory item objects, player race tagging, boss reward bundles, and combat stat derivation from equipped gear; updated `src/game/scenes/MissionScene.ts` so shields now come from equipped belts, enemy kills feed credits/materials, boss clears produce the proper loot bundle, and player combat stats read from equipment; refreshed `src/game/ui/InventoryOverlay.ts` so empty cargo/equipment, procedural item details, and material totals read correctly; and fixed the live main-menu title hierarchy in `src/game/scenes/MainMenuScene.ts`.
- Reward setup: demo boss progression now assumes `ember-watch` drops the legendary chest plus a procedural belt, `outpost-breach` drops the legendary legs plus a procedural weapon, and `nightglass-abyss` drops the legendary helm plus a procedural cape/back item.
- Verification: `npm run build` passes. `node tools/verify_loot_foundation_pass.mjs` passes and confirms: fresh new games start with empty equipment/cargo and zero shields; the main-menu title stack now renders at separated Y positions (`LoE 84`, `Pocket Legends: 126`, `The Circle of Light 204`, prototype line `258`); completing `ember-watch` creates a result bundle with a legendary chest + procedural belt + materials; returning to the hub adds those drops to cargo; and equipping the new belt raises shield capacity from `0` to `74`.
- 2026-04-08 follow-up 32: bumped to `pl_tcol_v0.0.42` for the world-loot and combat-effects pass.
- Implemented: aligned item rarity color rules in `src/game/content/items.ts` (`green/common`, `blue/rare`, `gold/legendary`, `purple/mythic`) and exported shared helpers so the same rarity language can drive UI, drops, and future crafting; extended `src/game/content/loot.ts` so boss reward bundles can be spawned into the world and mission-complete rewards only include the loot the player actually picked up; rebuilt mission loot handling in `src/game/scenes/MissionScene.ts` around visible world drops with glow halos, shadows, pickup prompts, boss loot explosions, auto-pickup credits, interact-to-pick materials/items, stronger combat flashes, shield sparks, heal/buff/debuff sigils, mobile interact-button support, and lightweight ambient stage lighting; added subtle fake lighting/shadow polish to the hub in `src/game/scenes/HubScene.ts`; expanded `src/game/audio/retroSfx.ts` with distinct drop/pickup cues for credits, common/rare/legendary/mythic loot and softened the baseline player-fire timbre; and updated `src/game/scenes/MainMenuScene.ts`, `src/game/scenes/UiVisionScene.ts`, `src/game/core/buildInfo.ts`, `index.html`, and `package.json` so the build now displays the new `pl_tcol_v0.0.42` naming.
- Verification: `npm run build` passes. `node tools/verify_loot_effects_pass.mjs` passes and confirms the main-menu build label reads `pl_tcol_v0.0.42`; enemy kills now spawn visible credit/material pickups with correct auto-pickup vs. interact rules; touch mode surfaces the new loot interact button; boss death creates a visible loot burst with legendary/rare items on the ground; picking up one boss item leaves the others behind and only the collected item appears on the mission-result reward list; and the new loot/drop/pickup SFX counters all fire as expected. Artifacts were saved under `output/web-game/loot-effects-pass/`.
- 2026-04-08 follow-up 33: bumped to `pl_tcol_v0.0.43` for mission inventory access and the mobile pickup-button pass.
- Implemented: extended `src/game/ui/InventoryOverlay.ts` so the same inventory/loadout layout can be fed by reusable snapshot data, supports read-only mission inspection, and stays pinned to the HUD layer when the mission camera is moving; added mission-side inventory access in `src/game/scenes/MissionScene.ts` through a new top-bar `Inventory` button plus `I` on keyboard; fed that mission overlay with live run-state data (banked gear, carried mission loot mirrored into cargo, and current run credits/materials summary); and changed mobile loot interaction so nearby item/material pickups now replace the attack button label/action directly while the old separate pickup button stays hidden. The mission scene now treats inventory as a real blocking overlay alongside the Data Pad so movement/combat input does not leak through under the menu.
- Verification: `npm run build` passes. `node tools/verify_mission_inventory_pass.mjs` passes and confirms the mission inventory opens from `I`, reports `allowEquip: false` for the run-view snapshot, shows recovered loot in cargo after pickup, and that touch-mode loot proximity changes the attack button to `Pick Up` while the separate interact button remains hidden. Artifacts were saved under `output/web-game/mission-inventory-pass/`.
- 2026-04-08 follow-up 34: bumped to `pl_tcol_v0.0.44` for extraction rules, lighting, pickup feedback, and a simple inventory stat readout.
- Implemented: added extraction banking in `src/game/core/session.ts` so early mission returns keep recovered credits/materials/items while death still loses unbanked run loot; wired pause-side extraction through `src/game/scenes/PauseScene.ts` and updated hub reward messaging in `src/game/scenes/HubScene.ts`; added a timed top-center pickup banner plus a shared reward-bundle helper in `src/game/scenes/MissionScene.ts`; extended `src/game/content/items.ts` with a compact combat-profile summary and surfaced it in `src/game/ui/InventoryOverlay.ts`; deepened ambient mood and edge shading through `src/game/ui/visualSettings.ts`; and pushed stronger additive glows, pickup halos, shield sparks, status sigils, and stage ambience in `MissionScene`/`HubScene`.
- Verification: `npm run build` passes. `node tools/verify_extraction_lighting_stats_pass.mjs` passes and confirms picked-up boss loot is banked when using early extraction (`cargoCount 0 -> 1`), the same style of recovered loot is lost on death (`cargoCount` unchanged on return from `game-over`), the timed pickup banner shows item names, and the inventory overlay now exposes a simple combat readout. Artifacts were saved under `output/web-game/extraction-lighting-stats-pass/`.
- Notes: the inventory overlay is materially cleaner now, but it still wants another layout polish pass before it becomes the final template for the rest of the UI family. The new lighting pass is moodier and more dramatic, but it is still stylized fake-lighting rather than a final authored art-lighting pass.
- 2026-04-08 follow-up 35: bumped to `pl_tcol_v0.0.45` for the real-lighting, junk-loot, and inventory-action pass.
- Implemented: converted salvage-style junk into real stackable cargo items in `src/game/content/items.ts`, `src/game/content/loot.ts`, and `src/game/core/session.ts` with five placeholder junk types (`Alloy Scrap`, `Filament Spool`, `Fractured Lens`, `Relay Core Fragment`, `Shadowglass Shard`) that now stack in cargo instead of behaving like a hidden currency bucket; extended `src/game/ui/InventoryOverlay.ts` with a real contextual item menu (`Equip` / `Unequip`, `Drop`, `Examine`) plus outside-click dismissal; rebuilt mission pickup handling in `src/game/scenes/MissionScene.ts` so enemy salvage becomes item pickups that feed mission cargo snapshots, credits still auto-pick up, projectile cores now glow, and combat/shield/support effects all route through stronger energy-particle and flash helpers; added a shared point-light + projected-shadow rig in `src/game/fx/energyLighting.ts`; and wired the hub into that same lighting model in `src/game/scenes/HubScene.ts` so the ship now has darker ambient mood, ceiling beams, live station/player/crew lights, and shadow-casting structures instead of almost no lighting presence.
- Verification: `npm run build` passes. The required shared web-game client ran successfully via `node C:\Users\Chase\.codex\skills\develop-web-game\scripts\web_game_playwright_client.js --url http://127.0.0.1:4173/?renderer=canvas --click 208,388 --iterations 1 --pause-ms 300 --screenshot-dir output/web-game/skill-client-real-lighting-final-2`. The deeper browser verifier `node tools/verify_real_lighting_junk_inventory_pass.mjs` plus a WebGL rerun confirmed: point-light/shadow rigs report `lightingSupported: true` in both `mission` and `hub`; hub ambient fixture lights are now present (`ambientLights: 4`); two separate junk pickups stack into one cargo stack (`Alloy Scrap x8`); boss loot enters cargo correctly; the inventory action menu opens for cargo items; equip moves the legendary chest into the `chest` slot; and unequip returns it to cargo. Artifacts were saved under `output/web-game/real-lighting-junk-inventory-pass/`.
- Notes: the new lighting path is now real point-light + shadow driven on WebGL, but it still wants more authored tuning once final art lands. Mission scenes can still look intentionally loud when multiple energy effects overlap in the exact same spot, especially in synthetic test setups that stack boss loot burst + shield spark + status sigils at once.
- 2026-04-08 follow-up 36: bumped to `pl_tcol_v0.0.46` for the lighting restraint, transient-cleanup, and reward-containment pass.
- Implemented: reined in the shared light rig in `src/game/fx/energyLighting.ts` so point lights are tighter, shadow graphics now multiply instead of just stacking alpha, and shadows read more clearly under a darker ambient base; tightened mission and hub lighting in `src/game/scenes/MissionScene.ts` and `src/game/scenes/HubScene.ts` by shrinking local radii, reducing bloom-heavy fixture glows, and adding boundary/cover casters so energy lights feel less like giant blobs and more like sources; added transient VFX tracking/cleanup in `MissionScene` so short-lived flashes, beams, rings, sigils, particles, and loot-burst shards are explicitly destroyed on completion and on room cleanup instead of leaving stray light traces behind; fixed the inventory contextual item menu in `src/game/ui/InventoryOverlay.ts` so it opens near the clicked slot/cell and is brought above the rest of the overlay; and rebuilt `src/game/scenes/MissionResultScene.ts` with a contained reward panel, aggregated loot lines, and a masked/scrollable recovered-loot area so long reward lists stay inside the mission-complete window instead of running through the button.
- Verification: `npm run build` passes. The required shared web-game client ran successfully via `node C:\Users\Chase\.codex\skills\develop-web-game\scripts\web_game_playwright_client.js --url http://127.0.0.1:4173/ --click 208,388 --iterations 1 --pause-ms 300 --screenshot-dir output/web-game/skill-client-cleanup-pass`. The focused verifier `node tools/verify_cleanup_fx_pass.mjs` confirmed the live browser can: keep the lighting rig active with shadow sources after the restraint pass, open the inventory action menu on a real gear item near the clicked cargo cell (`targetIndex: 1`, popup visible at `x: 512, y: 536`), and route into a mission-result scene carrying a long recovered-loot list without breaking scene flow. Artifacts were saved under `output/web-game/cleanup-fx-pass/`.
- Notes: headless WebGL screenshots are still unreliable for visual judging on this machine, so the verification here leans more on scene state and interaction paths than on screenshot fidelity. The next real visual polish pass should tune authored particle shapes and attack-light profiles now that the containment/cleanup layer is stable.
- 2026-04-08 follow-up 37: bumped to `pl_tcol_v0.0.47` for the effect-driven lighting cleanup pass.
- Implemented: removed persistent character/enemy glow from both ship and mission scenes by disabling the actor point lights in `src/game/scenes/MissionScene.ts` and `src/game/scenes/HubScene.ts`; retuned the ambient rigs so ship and mission areas are darker and less washed out, with tighter station/fixture beams and narrower stage lights; added transient shadow-light tracking in `MissionScene` so only attacks, projectiles, shield sparks, beams, pickups, and other short-lived energy effects contribute to the shadow pass; and kept those transient sources explicitly cleaned up so they stop leaving ghostly light trails behind after combat or room transitions.
- Verification: `npm run build` passes. `node tools/verify_lighting_model_pass.mjs` passes and confirms the player, companions, and enemies all report `lightVisible: false` while transient combat lights still feed the shadow rig (`transientShadowLights: 3`) and the ship keeps only ambient/station sources active. The required shared browser client also ran successfully against the preview build via `node C:\Users\Chase\.codex\skills\develop-web-game\scripts\web_game_playwright_client.js --url http://127.0.0.1:4173/ --click 208,388 --iterations 1 --pause-ms 300 --screenshot-dir output/web-game/skill-client-lighting-model-pass`.
- Notes: this pass is about discipline and separation, not the final authored VFX style. Actors no longer act like lanterns, and the game now reserves glow/shadow energy for projectiles, abilities, pickups, and fixture lighting. The next visual pass should shape specific attack lights and particles now that the baseline is darker and cleaner.
- 2026-04-08 follow-up 38: bumped to `pl_tcol_v0.0.48` for the static-glow removal and light-softening pass.
- Implemented: stripped the remaining non-effect glow from both worlds by removing mission fixture light sources, ship ambient beams, station highlight glows, and the airlock glow in `src/game/scenes/MissionScene.ts` and `src/game/scenes/HubScene.ts`; kept the mission/ship darkness layer but stopped feeding the shadow rig with static ship/station sources so shadows now come almost entirely from energy effects and loot; softened the shared light model in `src/game/fx/energyLighting.ts` by lowering point-light alpha, tightening falloff, and reducing shadow opacity; reduced projectile, support-beam, shield-spark, pickup, and general combat-flash light radii/intensity in `MissionScene`; and turned enemy aura fill into a near-zero tactical accent instead of a visible body glow so enemies no longer read like glowing sprites.
- Verification: `npm run build` passes. `node tools/verify_lighting_model_pass.mjs` passes and now reports `stageLightCount: 0`, `ambientLights: 0`, `shadowSourceCount: 0` in the hub, and lower mission-side shadow-source counts with actor lights still disabled. The required shared browser client also ran successfully via `node C:\Users\Chase\.codex\skills\develop-web-game\scripts\web_game_playwright_client.js --url http://127.0.0.1:4173/ --click 208,388 --iterations 1 --pause-ms 300 --screenshot-dir output/web-game/skill-client-lighting-prune-pass`.
- Notes: this is the stripped-down baseline the user asked for: no static world glow, no character-body glow, and softer effect/loot light. The next lighting step should be hand-shaping individual attack/projectile looks rather than turning the global brightness back up.
- 2026-04-08 follow-up 39: bumped to `pl_tcol_v0.0.49` for the lighting-balance rules pass.
- Implemented: added shared lighting presets in `src/game/fx/energyLighting.ts` so hero underlight, room fixtures, and shield glow now pull from one ruleset instead of scene-specific tuning; brightened the mission base slightly in `src/game/scenes/MissionScene.ts` while restoring soft fixture lights and hero-only underlights that respect wall occlusion; restored player and companion underlights in `src/game/scenes/HubScene.ts` along with subtle overhead room fixtures so the ship reads as lit again without returning to heavy bloom; and upgraded shield visuals in `MissionScene` so active shields have a soft fill glow plus stronger recharge pulsing while keeping enemy body glow off.
- Verification: `npm run build` passes. `node tools/verify_lighting_model_pass.mjs` passes and now reports `playerLightVisible: true`, `companionLightsVisible: [true, true, true]`, `enemyLightsVisible: [false]`, `stageLightCount: 6`, `ambientLights: 4`, and `hub shadowSourceCount: 11`. The required shared browser client also ran successfully via `node C:\Users\Chase\.codex\skills\develop-web-game\scripts\web_game_playwright_client.js --url http://127.0.0.1:4173/ --click 208,388 --iterations 1 --pause-ms 300 --screenshot-dir output/web-game/skill-client-hero-room-light-pass`.
- Notes: this is the new baseline rule set for future content: world stays moody, heroes get soft underlights, rooms get subtle fixture light, enemies do not passively glow, shields have a readable soft aura, and attacks/projectiles/status/loot remain the transient light sources. The next pass should be shaping the exact look of a few specific attacks now that the shared lighting rules are back in balance.
- 2026-04-09 follow-up 40: bumped to `pl_tcol_v0.0.50` for the first real 16:9 UI-overhaul milestone.
- Implemented: added shared grid/layout helpers in `src/game/ui/layoutGrid.ts` and a hidden layout inspector in `src/game/ui/LayoutDebugOverlay.ts`; rebuilt `src/game/ui/InventoryOverlay.ts` around the new 16:9 Data Pad shell with the user-authored tab row, centered character well, grouped equipment block, single-row cargo band, centered item popup, and `F7` debug overlay; rebuilt `src/game/ui/LogbookOverlay.ts` onto the same shell so the mission list/detail screen now follows the same top-bar/content-window rules as inventory; wired both overlays into `src/game/scenes/HubScene.ts` and `src/game/scenes/MissionScene.ts` with shared tab routing between `Inventory` and `Missions`; and updated the layout manual in `design/ui-layouts/manuals/UI SYSTEM MANUAL — CORE PRINCIPLES.txt` to use engine-agnostic container language instead of Godot-specific `Control node` wording.
- Verification: `npm run build` passes. The required shared web-game client ran successfully in canvas mode via `node C:\Users\Chase\.codex\skills\develop-web-game\scripts\web_game_playwright_client.js --url http://127.0.0.1:5173/?renderer=canvas --click 210,388 --iterations 1 --pause-ms 700 --screenshot-dir output/web-game/ui-overhaul-canvas-smoke`. The focused verifier `node tools/verify_ui_layout_pass.mjs` passes and captures the new `inventory`, `inventory-debug`, `missions-empty`, `missions-routed`, and `inventory-return` screens under `output/web-game/ui-layout-pass/`, confirming the new overlays render in the hub, the hidden debug grid can be toggled, missions can be routed and displayed in the new log shell, and the scene-driven tab routing returns from the logbook back to inventory.
- Notes: this is intentionally the first shell/layout milestone, not the final skin pass. The `Skills`, `Map`, and `Starship` tabs are now scaffolded in the shared Data Pad shell but left inactive until their content areas are rebuilt. Automated canvas clicks on pinned tab buttons are still a little flaky in Playwright, so the current verifier uses direct scene routing for tab swaps while the new shells themselves are inspected visually.
- 2026-04-18 follow-up 41: rebuilt the ship hub scene around the SHIP_HUB slice floorplan instead of the old one-room command deck.
- Implemented: reshaped `src/game/scenes/HubScene.ts` into a ship-style walkable footprint with cockpit, main bay, engineering, crew, and exit-hatch zones; replaced the old `cockpit/mission/loadout` station set with `pilot seat`, `ship systems`, `mission terminal`, `cargo crates`, `forge`, `repair terminal`, and `crew terminal`; moved the deploy interaction to a bottom-center `Exit Hatch`; repositioned the hub roster into the crew room; added real walk-zone movement bounds so the player is constrained to the ship silhouette; and wired the new stations either into existing overlays (`Mission Terminal`, `Crew -> deploy board`, `Cargo -> inventory shortcut`) or live placeholder panels backed by ship/session data (`Pilot Seat`, `Ship Systems`, `Repair`, `Forge`).
- Verification: `npm run build` passes. Ran the required shared web-game client against a local preview build in canvas mode after bridging the skill's Playwright resolution on Windows. Fresh artifacts were captured under `output/web-game/ship-hub-layout-pass-client-hub-2/` for the hub view and `output/web-game/ship-hub-layout-pass-client-mission/` for mission-terminal interaction; the latter confirms the rebuilt station layout still opens the mission overlay from the player's spawn area.
- Notes: the hub now matches the intended room plan structurally, but some text density remains in the crew room because six companion labels plus the crew terminal are sharing a small footprint. The next pass should either build the dedicated crew window or simplify on-floor crew label presentation so that room reads cleaner.
- Verification addendum: direct browser automation via the debug-exposed `window.__loeGame` opened the new `Pilot Seat` placeholder panel and captured `output/web-game/ship-hub-layout-pass-pilot-panel.png`, confirming one of the newly added room-specific placeholder consoles renders correctly even without the SPACE slice implemented yet.
- 2026-04-18 follow-up 42: rescaled the ship hub after layout feedback so the rooms read as one connected ship instead of detached blocks.
- Implemented: rebuilt the `HubScene` ship footprint again with a larger cockpit/main-bay/side-bay scale; made the main bay one continuous rectangle instead of two stacked green sections; attached engineering and crew bays to the main bay with interior wall segments plus visible opening frames; shifted the player spawn lower into the center of the enlarged main bay; shrank every terminal/station footprint so the rooms feel less crowded; and tightened crew-label font size/placement so the right bay is more readable.
- Verification: `npm run build` passes. The required shared web-game client captured the updated hub under `output/web-game/ship-hub-layout-pass-client-hub-3/`, which now shows a single main bay, attached side bays, and smaller terminals. Direct browser automation also moved the player to the mission terminal and captured `output/web-game/ship-hub-layout-pass-mission-click.png`, confirming the mission console still opens correctly after the rescale.
- Notes: this is structurally much closer to the slice image now. Remaining visual debt is mostly readability polish, especially the companion-label density in crew quarters and the general header/status text overlap at the very top of the screen.
- 2026-04-18 follow-up 43: adjusted the ship hub again so the central bay now reads as a vertical spine instead of a horizontal room split.
- Implemented: updated `src/game/scenes/HubScene.ts` so the cockpit and main bay now share the same width, the main bay extends vertically down the ship centerline, engineering and crew bays attach into that center spine, and the previous non-walkable blockers around the bay transitions were removed in favor of broader connected walk zones. Shrunk the station footprints again so the center path stays readable, kept the placeholder/panel wiring intact for `Pilot Seat`, `Ship Systems`, `Forge`, `Repair`, `Cargo`, and `Crew`, and preserved the bottom-center `Exit Hatch` deploy path.
- Verification: `npm run build` passes. The updated preview layout was exercised through the shared browser client in `output/web-game/ship-hub-layout-pass-client-hub-4/`, and direct browser automation still opens the mission console successfully after the geometry change in `output/web-game/ship-hub-layout-pass-mission-click-vertical.png`.
- Notes: this pass prioritizes slice-complete flow over collision polish, so the ship now plays as one connected interior even though the doorway thresholds are currently generous. That is intentional for this slice and can be tightened later once the full interaction set is stable.
- 2026-04-18 follow-up 44: fixed the room-transition bug that was still blocking access into the side bays.
- Implemented: updated `src/game/scenes/HubScene.ts` so the walk zones now overlap instead of only touching at their edges, which removes the invisible collision gaps created by the player-radius clamp. The central spine now runs as one continuous walkable column and both side bays overlap into it enough to pass cleanly. Also softened the `addDoorFrame` art so the openings read as open/passable thresholds instead of solid blocked doors.
- Verification: `npm run build` passes. The shared web-game client now reaches engineering successfully with the player landing at `x: 273, y: 430` in `output/web-game/ship-hub-bay-door-fix-left/state-0.json`. A focused Playwright verifier in `output/web-game/verify_ship_hub_bays.mjs` confirmed both sides end-to-end: engineering reached at `x: 274` and crew reached at `x: 1006`, with screenshots under `output/web-game/ship-hub-bay-door-fix-verify/`.
- Notes: the remaining doorway visuals are intentionally simple. The important part for this slice is that the ship now navigates as one connected interior; if we later want animated doors, they can be added on top of the now-correct room connectivity.
- 2026-04-18 follow-up 45: version display is now sourced from `package.json` instead of scattered hardcoded strings.
- Implemented: rebuilt `src/game/core/buildInfo.ts` so `GAME_VERSION`, `GAME_BUILD`, and the new `GAME_WINDOW_TITLE` are derived directly from `package.json.version`; updated `src/main.ts` to set the browser/window title from that same shared value at startup; and trimmed `index.html` back to a generic fallback title so it no longer carries a stale hardcoded build number.
- Verification: `npm run build` passes. Main-menu browser verification confirmed `document.title` resolves to `Pocket Legends: The Circle of Light pl_tcol_v0.0.50`, `render_game_to_text()` reports `snapshot.version: pl_tcol_v0.0.50`, and the captured menu screenshot under `output/web-game/main-menu-version-sync/shot-0.png` shows the same build label on screen.
- Notes: from this point forward, bumping `package.json.version` is enough to update the visible main-menu build string, the debug snapshot version, and the browser tab title together. No separate menu-string edit is needed anymore.
- 2026-04-18 follow-up 46: bumped the project build number to `pl_tcol_v0.0.55` to reflect the accumulated ship-hub and versioning work.
- Implemented: ran `npm version 0.0.55 --no-git-tag-version`, which updated both `package.json` and `package-lock.json`. Because build labels now derive from `package.json.version`, the main-menu build string, debug snapshot version, and browser title all moved to `pl_tcol_v0.0.55` automatically with no extra string edits.
- Verification: `npm run build` passes. The required shared web-game client captured the updated main menu under `output/web-game/main-menu-version-sync-v055/`, and direct browser verification confirmed `document.title` is `Pocket Legends: The Circle of Light pl_tcol_v0.0.55` while `render_game_to_text()` reports `snapshot.version: pl_tcol_v0.0.55`.
- Notes: future version bumps should keep using the package version as the only edit point. The visible menu/build label is now following that source of truth as intended.
- 2026-04-18 follow-up 47: built the first playable SPACE foundation pass and wired it into the ship hub.
- Implemented: added shared space-world config and field generation in `src/game/content/space.ts` with a single large-world source of truth (`16000 x 16000`, spawn at center, seeded stars + breakables); created `src/game/scenes/SpaceScene.ts` as a top-down 2D asteroids-style flight/combat state with camera-locked player ship, WASD arcade movement, mouse aim, LMB/Space firing, floating asteroid/debris targets, destruction bursts, and a clean return-to-ship control; registered the new scene in `src/game/createGame.ts`; and updated `src/game/scenes/HubScene.ts` so the pilot seat now opens a real launch panel and transitions into the new space slice instead of the old placeholder messaging while keeping the current exit-hatch ground shortcut intact.
- Verification: `npm run build` passes. Ran the required shared web-game client against the preview build in canvas mode (`output/web-game/space-slice-skill-client-smoke-3/`). Added and ran `node tools/verify_space_slice_pass.mjs`, which verifies `hub -> pilot seat -> space -> movement -> shooting -> asteroid destruction -> return to hub` end to end and writes artifacts to `output/web-game/space-slice-pass/`. Latest verification confirms: pilot seat is the active interaction target, the panel action is `Launch Into Space`, the space world loads at `16000 x 16000`, camera center stays locked to ship center, ship movement changes both position and velocity, at least one asteroid/debris object is destroyed by live fire, return routes cleanly back to hub, and the prior hub re-entry lighting error is gone (`consoleErrors: []`).
- Notes: this pass intentionally does not add enemies, planets, landing, sectors, mission destinations, or deeper travel logic yet. The exit hatch still acts as the temporary direct ground-combat shortcut until landing/return flow is built. Minor UI polish debt remains in the pilot-seat overlay because the underlying hub status text can still read through around the panel area.
- TODO next: connect accepted mission routes more explicitly into space travel state/UI, decide whether the first follow-up is enemy ships or destination/landing flow, and do a light readability pass on the pilot-seat panel/header layering once the next space features are in.
- 2026-04-18 follow-up 48: bumped the live browser build to `pl_tcol_v0.0.56` and pushed the updated bundle back onto the local preview page.
- Implemented: ran `npm version 0.0.56 --no-git-tag-version`, which updated `package.json` and `package-lock.json`. Because visible build info is sourced from `package.json.version`, the main-menu version label and browser window title advanced automatically with no extra string edits.
- Verification: `npm run build` passes on `0.0.56`. Restarted the local preview server on `http://127.0.0.1:4173/` and ran the required shared web-game client in canvas mode with artifacts under `output/web-game/main-menu-version-sync-v056/`. Direct browser verification confirmed `document.title` is `Pocket Legends: The Circle of Light pl_tcol_v0.0.56`, `render_game_to_text()` reports `snapshot.version: pl_tcol_v0.0.56`, and the captured screenshot shows `pl_tcol_v0.0.56` on the main menu.
- Notes: preview server was intentionally left running so the browser page is already serving the updated build.
- 2026-04-18 follow-up 49: bumped the live browser build to `pl_tcol_v0.0.57` for the first faction-ships pass in the space slice.
- Implemented: extended `src/game/content/space.ts` with faction definitions, sector ship-spawn rules, and a new `Trade Drift Core` spawn sector so launch space starts with nearby traffic instead of empty void; rebuilt `src/game/scenes/SpaceScene.ts` to add Empire, Pirate, Republic, and Smuggler ships as simple color-coded actors with hull, firing, destruction bursts, sector patrols, readable target selection, and local-contact HUD reporting; kept Smugglers neutral by default with flee-first behavior plus retaliation when attacked; and connected space death into the existing `src/game/scenes/GameOverScene.ts` flow so destroyed player ships route through the shared GAME OVER screen instead of a separate space-only fail state.
- Verification: `npm run build` passes on `0.0.57`. Ran the required shared web-game client against the live preview page after the rebuild in `output/web-game/main-menu-version-sync-v057-final/`. `node tools/verify_space_faction_pass.mjs` passes and confirms: all four factions spawn, sector distribution is regional instead of evenly mixed, Empire/Pirate ships treat the player as hostile, Republic ships do not target the player by default, Smugglers stay neutral until provoked, faction ships exchange fire, and space death routes into `game-over` with `mode: "space"`. Direct browser verification also confirms the preview page title and main-menu snapshot both read `pl_tcol_v0.0.57`.
- Notes: hostile player-targeting now takes priority whenever the player is inside an Empire/Pirate provoked-Smuggler ship's detect range, which keeps launch space reliably dangerous while still allowing faction-vs-faction fights whenever the player is outside the engagement bubble. The local preview server was restarted after the rebuild so `http://127.0.0.1:4173/` is serving the updated bundle.
- 2026-04-18 follow-up 50: repaired the GitHub Pages deployment path so browser releases do not drift behind localhost-only preview builds.
- Implemented: updated `vite.config.ts` to resolve the Pages base automatically from `DEPLOY_BASE` or the GitHub Actions repository slug while making `dist/` the explicit build output; added dedicated Pages scripts in `package.json` plus `tools/build_pages.mjs` and `tools/verify_pages_build.mjs` so the `/loe_col/` artifact can be built and checked locally before publish; changed `.github/workflows/deploy-pages.yml` to run the explicit Pages build and verification steps instead of relying on an easy-to-miss ad hoc env override; and documented the release flow in `docs/DEPLOYMENT_WORKFLOW.md` with a short link from `README.md`.
- Verification: `npm run build` passes for localhost preview, `npm run build:pages` passes for the GitHub Pages artifact, and `npm run verify:pages-build` confirms `dist/index.html` points at `/loe_col/assets/...` instead of root `/assets/...`. Browser verification also confirmed the normal localhost production preview still loads the main menu at `pl_tcol_v0.0.57`.
- Notes: the actual GitHub Pages site only updates after a commit is pushed to `main`, because the deploy workflow is intentionally triggered by `push` or manual dispatch. The earlier drift happened because the recent work had only been built and previewed locally and had never reached `origin/main`, so Pages had nothing new to publish.
